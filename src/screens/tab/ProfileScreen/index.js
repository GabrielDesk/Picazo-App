import React from 'react';
import { SafeAreaView, Text, View } from 'react-native';

export const ProfileScreen = ({ navigation }) => {
  return (
    <SafeAreaView style={{ height: '100%', backgroundColor: 'blue' }}>
      <Text>ProfileScreen</Text>
    </SafeAreaView>
  );
};
